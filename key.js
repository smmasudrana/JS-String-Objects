const computer={
    brand:'lenevo',
    price:'3500',
    processor:'intel',
    hdd:'512gb',
}

const keys =Object.keys(computer);   //when you want to know how many key/property in the object.
console.log(keys);

const value =Object.values(computer);   //when you want to know how many values in the object.
console.log(value);